[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tcm-piv"
version = "0.1.0"
description = "Particle image velocimetry toolbox for the Twente Cough Machine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []